import HomeView from '~/components/Home/HomeView';

/** [EXT] /home route — Navvia dashboard. Opt-in via librechat.yaml interface.home: 'dashboard'. */
export default function HomeRoute() {
  return <HomeView />;
}
